rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // All data access is mediated by Next.js API routes (Admin SDK).
      // Client SDK direct read/write is denied by default.
      allow read, write: if false;
    }
  }
}
